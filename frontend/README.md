# SecU Dashboard (React + Tailwind) - Dummy Frontend Scaffold

This is a minimal scaffold containing dummy React components and pages
for every sidebar item shown in the provided design. It's intended
as a starting point — replace dummy content with real UI / data.

How to run:
1. npm install
2. npm run dev

Files included: src/ (components, pages, layouts, routes, assets)
